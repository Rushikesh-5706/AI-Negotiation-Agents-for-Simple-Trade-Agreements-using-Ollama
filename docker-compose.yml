services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - ollama
    volumes:
      - ./data:/app/data
    env_file:
      - .env

volumes:
  ollama_data:
